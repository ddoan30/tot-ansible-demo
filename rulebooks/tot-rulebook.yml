---
- name: Monitor httpd and trigger Arduino remediation
  hosts: vm1.lab.local
  sources:
    - ansible.eda.url_check:
        urls:
          - http://192.168.122.149
        delay: 1

  rules:
    - name: If httpd is unreachable, deploy Arduino code
      condition: event.url_check.status == "down"
      action:
        run_job_template:
          name: "remediate"
          organization: "Default"
